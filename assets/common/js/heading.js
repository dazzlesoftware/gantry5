(() => {'use strict';const done=new WeakSet(),loaded=new Set();const init=(scope=document)=>{const roots=scope.matches?.('[data-heading]')?[scope]:scope.querySelectorAll?.('[data-heading]')||[];roots.forEach(root=>{if(done.has(root))return;done.add(root);const font=String(root.dataset.font||'').trim();if(!font)return;let family=font;if(font.startsWith('family=')){const params=new URLSearchParams(font);family=(params.get('family')||'').replace(/\+/g,' ').split(':')[0];const href=`https://fonts.googleapis.com/css?${font}`;if(family&&!loaded.has(href)){const link=document.createElement('link');link.rel='stylesheet';link.href=href;document.head.append(link);loaded.add(href)}}if(family)root.querySelector('.g-heading').style.fontFamily=`'${family.replace(/'/g,"\\'")}'`;})};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>init(),{once:true}):init();new MutationObserver(records=>records.forEach(record=>record.addedNodes.forEach(node=>node.nodeType===1&&init(node)))).observe(document.documentElement,{childList:true,subtree:true});})();
