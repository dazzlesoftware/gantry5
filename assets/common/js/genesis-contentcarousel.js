(()=>{'use strict';const done=new WeakSet();const videoUrl=value=>{try{const url=new URL(value,document.baseURI),host=url.hostname.replace(/^www\./,'').toLowerCase();let id='';if(host==='youtu.be')id=url.pathname.split('/').filter(Boolean)[0]||'';else if(host==='youtube.com'||host==='m.youtube.com'||host==='youtube-nocookie.com'){if(url.pathname==='/watch')id=url.searchParams.get('v')||'';else{const parts=url.pathname.split('/').filter(Boolean);if(['embed','shorts','live'].includes(parts[0]))id=parts[1]||''}}if(id){const embed=new URL(`https://www.youtube-nocookie.com/embed/${encodeURIComponent(id)}`);const start=url.searchParams.get('start')||url.searchParams.get('t');if(start&&/^\d+$/.test(start))embed.searchParams.set('start',start);return embed.href}return url.href}catch{return''}};const init=root=>{if(done.has(root))return;done.add(root);root.querySelectorAll('[data-carousel-video]').forEach(frame=>{const source=videoUrl(frame.dataset.carouselVideo);if(source)frame.src=source;else{frame.removeAttribute('src');frame.setAttribute('aria-hidden','true')}});const track=root.querySelector('.g-genesis-carousel-track'),slides=[...root.querySelectorAll('.g-genesis-carousel-slide')];if(!track||!slides.length)return;if(root.dataset.randomize==='enable')slides.sort(()=>Math.random()-.5).forEach(s=>track.append(s));let index=0,timer;const dots=root.querySelector('[data-carousel-dots]');if(dots)dots.replaceChildren(...slides.map((_,i)=>{const b=document.createElement('button');b.type='button';b.setAttribute('aria-label',`Show slide ${i+1}`);b.onclick=()=>show(i,true);return b}));const show=(next,reset=false)=>{index=root.dataset.loop==='enable'?((next%slides.length)+slides.length)%slides.length:Math.max(0,Math.min(next,slides.length-1));track.style.transitionDuration=`${Math.max(0,+root.dataset.speed||500)}ms`;track.style.transform=`translate3d(-${index*100}%,0,0)`;slides.forEach((s,i)=>s.setAttribute('aria-hidden',String(i!==index)));if(dots)[...dots.children].forEach((d,i)=>d.classList.toggle('active',i===index));if(reset)start(true)};const start=(reset=false)=>{if(reset){clearInterval(timer);timer=null}if(root.dataset.autoplay==='enable'&&!timer&&slides.length>1)timer=setInterval(()=>show(index+1),Math.max(1000,+root.dataset.interval||5000))};root.querySelector('[data-carousel-prev]')?.addEventListener('click',()=>show(index-1,true));root.querySelector('[data-carousel-next]')?.addEventListener('click',()=>show(index+1,true));if(root.dataset.pauseHover==='enable'){root.addEventListener('mouseenter',()=>{clearInterval(timer);timer=null});root.addEventListener('mouseleave',()=>start())}let x;root.addEventListener('pointerdown',e=>x=e.clientX);root.addEventListener('pointerup',e=>{if(x!=null&&Math.abs(e.clientX-x)>40)show(index+(e.clientX<x?1:-1),true);x=null});show(0);start()};const scan=(s=document)=>(s.matches?.('[data-genesis-carousel]')?[s]:s.querySelectorAll?.('[data-genesis-carousel]')||[]).forEach(init);document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>scan(),{once:true}):scan();new MutationObserver(rs=>rs.forEach(r=>r.addedNodes.forEach(n=>n.nodeType===1&&scan(n)))).observe(document.documentElement,{childList:true,subtree:true})})();
