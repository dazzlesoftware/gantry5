(()=>{'use strict';const done=new WeakSet(),fonts=new Set();const font=(root,selector,value)=>{value=String(value||'').trim();if(!value)return;let family=value;if(value.startsWith('family=')){const p=new URLSearchParams(value);family=(p.get('family')||'').replace(/\+/g,' ').split(':')[0];const href=`https://fonts.googleapis.com/css?${value}`;if(family&&!fonts.has(href)){const l=document.createElement('link');l.rel='stylesheet';l.href=href;document.head.append(l);fonts.add(href)}}root.querySelector(selector).style.fontFamily=family};const init=root=>{if(done.has(root))return;done.add(root);font(root,'.g-animated-number-value',root.dataset.numberFont);if(root.querySelector('.g-animated-number-title'))font(root,'.g-animated-number-title',root.dataset.titleFont);const target=Number(root.dataset.number||0),fraction=String(root.dataset.fraction||''),duration=Math.max(0,Number(root.dataset.duration||2000)),out=root.querySelector('[data-number-value]'),format=value=>{let s=String(Math.round(value));if(root.dataset.format==='enable')s=s.replace(/\B(?=(\d{3})+(?!\d))/g,root.dataset.separator||',');return s+(fraction?(root.dataset.fractionSeparator||'.')+fraction:'')};const run=()=>{const start=performance.now();const tick=now=>{const p=duration?Math.min(1,(now-start)/duration):1;out.textContent=format(target*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)};if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){io.disconnect();run()}}),{threshold:.2});io.observe(root)}else run()};const scan=(s=document)=>(s.matches?.('[data-animated-number]')?[s]:s.querySelectorAll?.('[data-animated-number]')||[]).forEach(init);document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>scan(),{once:true}):scan();new MutationObserver(rs=>rs.forEach(r=>r.addedNodes.forEach(n=>n.nodeType===1&&scan(n)))).observe(document.documentElement,{childList:true,subtree:true})})();
