{% extends "partials/page_head.html.twig" %}

{% block head_application -%}
    <meta charset="utf-8" />
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    <title>{{ errorcode|default(500) }} {{ error|default('GENESIS_ENGINE_UNKNOWN_ERROR'|trans) }}</title>
    {% do genesis.document.errorPage(true) -%}
    {% do genesis.platform.finalize() -%}
    {{ genesis.styles('head', true)|join("\n    ")|raw }}
    {{ genesis.scripts('head', true)|join("\n    ")|raw }}
{%- endblock %}
