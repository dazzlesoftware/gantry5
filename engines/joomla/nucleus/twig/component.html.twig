{% extends "partials/page.html.twig" %}

{% block page_body -%}
    <body{{ genesis.page.bodyAttributes({'class': [offcanvas_position, genesis.page.preset, 'g-style-' ~ genesis.theme.preset]})|raw }}>
    {{ genesis.config.page.body.body_top|raw }}
    {{ page_top|raw }}
    {{ page_layout|raw }}
    {{ page_bottom|raw }}
    {{ page_footer|raw }}
    {{ genesis.config.page.body.body_bottom|raw }}
    </body>
{%- endblock %}

{% block page_layout %}
    {% do genesis.theme.joomla(true) %}
    <div class="platform-content row-fluid">
        <div class="span12">
            <jdoc:include type="message" />
            <jdoc:include type="component" />
        </div>
    </div>
{% endblock %}
