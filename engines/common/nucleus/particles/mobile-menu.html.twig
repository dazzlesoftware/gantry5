{% extends '@nucleus/partials/particle.html.twig' %}

{% block particle %}
    {# Optional compatibility mount point for saved outlines. Bootstrap
       Offcanvas owns the drawer; the shared layout supplies the same mount
       automatically when this particle is disabled or absent. #}
    <div id="g-mobilemenu-container" class="g-mobilemenu" data-g-menu-breakpoint="{{ genesis.config.get('styles.breakpoints.mobile-menu-breakpoint')|default('48rem') }}"></div>
{% endblock %}
