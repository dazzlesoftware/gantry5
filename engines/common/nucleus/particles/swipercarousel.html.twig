{% extends '@nucleus/partials/particle.html.twig' %}

{% block particle %}
    {% styles %}
        <link rel="stylesheet" href="{{ url('gantry-assets://css/swiper.css') }}" type="text/css" />
    {% endstyles %}

    {% set navigationEnabled = particle.nav in ['enable', 'enabled', true, 1, '1'] %}
    {% set paginationEnabled = particle.dots in ['enable', 'enabled', true, 1, '1'] %}

    <div class="{{ particle.class|e }}">
        {% if particle.title %}<h2 class="g-title">{{ particle.title|raw }}</h2>{% endif %}

        <div
            id="g-swipercarousel-{{ id }}"
            class="g-swipercarousel swiper {% if particle.imageOverlay == 'enable' %}has-color-overlay{% endif %}"
            data-g-swiper
            data-slides-per-view="{{ particle.displayitems|default(1)|e }}"
            data-navigation="{{ particle.nav|default('disable')|e }}"
            data-pagination="{{ particle.dots|default('enable')|e }}"
            data-loop="{{ particle.loop|default('enable')|e }}"
            data-autoplay="{{ particle.autoplay|default('disable')|e }}"
            data-autoplay-delay="{{ particle.autoplaySpeed|default(5000)|e }}"
            data-pause-on-hover="{{ particle.pauseOnHover|default('enable')|e }}"
            data-speed="{{ particle.transitionSpeed|default(500)|e }}"
            data-effect="{{ particle.effect|default('slide')|e }}"
            data-previous-label="Previous slide"
            data-next-label="Next slide"
        >
            {% for item in particle.items|default([]) %}
                {% if not item.disable %}
                    <div class="g-swipercarousel-item {{ item.class|e }}">
                        <div class="g-swipercarousel-item-wrapper">
                            {% if item.image %}
                                <div class="g-swipercarousel-item-img">
                                    <img src="{{ url(item.image)|e }}" alt="{{ item.title|striptags|e }}" loading="lazy" />
                                </div>
                            {% endif %}
                            <div class="g-swipercarousel-item-content-container">
                                <div class="g-swipercarousel-item-content-wrapper">
                                    <div class="g-swipercarousel-item-content">
                                        {% if item.title %}
                                            <h1 class="g-swipercarousel-item-title">{{ item.title|raw }}</h1>
                                        {% endif %}
                                        {% if item.desc %}
                                            <h2 class="g-swipercarousel-item-desc">{{ item.desc|raw }}</h2>
                                        {% endif %}
                                        {% if item.link %}
                                            <div class="g-swipercarousel-item-link">
                                                <a
                                                    target="{{ item.buttontarget|default('_self')|e }}"
                                                    {% if item.buttontarget|default('_self') == '_blank' %}rel="noopener noreferrer"{% endif %}
                                                    class="g-swipercarousel-item-button button {{ item.buttonclass|e }}"
                                                    href="{{ item.link|e }}"
                                                >
                                                    {{ item.linktext|raw }}
                                                </a>
                                            </div>
                                        {% endif %}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}

            {% if navigationEnabled %}
            <div class="swiper-navigation">
                <button class="swiper-button-prev" type="button" aria-label="Previous slide">
                    {{ particle.prevText|default('<i class="fa fa-chevron-left" aria-hidden="true"></i>')|raw }}
                </button>
                <button class="swiper-button-next" type="button" aria-label="Next slide">
                    {{ particle.nextText|default('<i class="fa fa-chevron-right" aria-hidden="true"></i>')|raw }}
                </button>
            </div>
            {% endif %}
            {% if paginationEnabled %}
            <div class="swiper-pagination"></div>
            {% endif %}
        </div>
    </div>
{% endblock %}

{% block javascript_footer %}
    {% do gantry.document.addScript(url('gantry-assets://js/swiper.js'), 11, 'footer') %}
{% endblock %}
