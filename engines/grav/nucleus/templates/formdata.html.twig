{% extends "partials/page.html.twig" %}

{% block content %}
    {{ content|raw }}

    <div class="alert">{{ form.message }}</div>

    <p>{{ 'GENESIS_ENGINE_FORM_SUMMARY'|trans }}</p>

    {% include "forms/data.html.twig" %}
{% endblock %}
