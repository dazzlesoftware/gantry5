{% extends ajax-suffix ? "@genesis-admin/partials/ajax.html.twig" : "@genesis-admin/partials/base.html.twig" %}

{% set settings_url = genesis.platform.settings %}
{% set settings_key = genesis.platform.settings_key %}

{% block content %}
    <div id="genesis-container" data-genesis-container="" class="container">
        <div class="inner-container">
            <div class="row">
                <div class="col main-block">
                    <section id="main">
                        <div data-genesis-content="">
                            {% block genesis %}
                                <div class="genesis-content" data-genesis-content="">
                                    <div class="row overview-header">
                                        <div class="col">
                                            <h2 class="theme-title"><i class="fa fa-fw fa-plug" aria-hidden="true"></i> {{ 'GENESIS_PLATFORM_AVAILABLE_THEMES'|trans }}</h2>
                                        </div>
                                    {% if settings_url %}
                                        <div class="col">
                                            <a class="button button-primary float-right" href="{{ settings_url }}" data-settings-key="{{ settings_key }}">
                                                <i class="fa fa-cog" aria-hidden="true"></i> {{ 'GENESIS_PLATFORM_PLATFORM_SETTINGS'|trans }}
                                            </a>
                                        </div>
                                    {% endif %}
                                    </div>
                                    <div class="themes cards-wrapper row fixed-blocks">
                                        {% if themes|length %}
                                        {% for id, theme in themes %}
                                            <div class="theme card">
                                                <div class="theme-id">
                                                    {% if theme.details.icon %}<i class="fa fa-fw fa-{{ theme.details.icon }}" aria-hidden="true"></i>{% endif %}
                                                    {{ theme.title }} <span class="theme-info">(v{{ theme.details.version }} / {{ theme.name }})</span>
                                                </div>
                                                <div class="theme-screenshot">
                                                    {% if theme.admin_url %}
                                                    <a href="{{ theme.admin_url }}">
                                                    {% endif %}
                                                        <img src="{{ url(theme.thumbnail)|default('http://www.placehold.it/206x150/f4f4f4') }}" />
                                                    {% if theme.admin_url %}
                                                    </a>
                                                    {% endif %}
                                                </div>
                                                <div class="theme-name">
                                                    {% if theme.preview_url %}
                                                    <a class="button" href="{{ theme.preview_url }}" target="_blank">{{ 'GENESIS_PLATFORM_PREVIEW'|trans }}</a>
                                                    {% endif %}
                                                    {% if theme.admin_url %}
                                                    <a class="button button-primary" href="{{ theme.admin_url }}">{{ 'GENESIS_PLATFORM_CONFIGURE'|trans }}</a>
                                                    {% endif %}
                                                </div>
                                            </div>
                                        {% endfor %}
                                        {% else %}
                                            <p>{{ 'GENESIS_PLATFORM_NO_THEMES_INSTALLED'|trans }}</p>
                                        {% endif %}
                                    </div>
                                </div>
                            {% endblock %}
                        </div>
                    </section>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block footer_section %}
{% endblock %}
