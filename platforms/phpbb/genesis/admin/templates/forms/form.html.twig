{% set form_id = form_id ? form_id : 'genesisform' %}

<form id="{{ form_id }}" method="post">
    {% include 'forms/fields.html.twig' %}

    <div class="button-bar">
        <button class="button" name="task" value="save">{{ 'GENESIS_PLATFORM_SAVE'|trans }}</button>
    </div>
</form>
