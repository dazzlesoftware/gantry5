{% extends ajax-suffix ? "@genesis-admin/partials/ajax.html.twig" : "@genesis-admin/partials/base.html.twig" %}

{% block genesis %}
    <div id="configurations">
        <div class="menu-header">
        <span class="float-right">
            <button class="button button-back-to-conf" tabindex="0"
                    aria-label="{{ 'GENESIS_PLATFORM_BACK_SETUP'|trans }}"><i class="fa fa-fw fa-fw fa-arrow-left" aria-hidden="true"></i>
                <span>{{ 'GENESIS_PLATFORM_BACK_SETUP'|trans }}</span></button>
        </span>
            <h2 class="page-title">
                {{ 'GENESIS_PLATFORM_OUTLINES'|trans }}
                {% if genesis.authorize('outline.create') %}
                <a href="{{ genesis.route('configurations', 'create') }}"
                   title="{{ 'GENESIS_PLATFORM_ADD_NEW_OUTLINE'|trans }}"
                   aria-label="{{ 'GENESIS_PLATFORM_ADD_NEW_OUTLINE'|trans }}"
                   data-genesis-outline-create class="button button-primary">
                    <i class="fa fa-fw fa-plus" aria-hidden="true"></i>
                </a>
                {% endif %}
            </h2>
        </div>

        <ul class="g-grid">
            <li class="card g-block size-1-4">
                {% set name = 'default' %}
                {% include '@genesis-admin/layouts/outline.html.twig' %}
            </li>
            {% for name, title in genesis.outlines.user %}
                <li class="card g-block size-1-4{{ genesis.outlines.isDefault(name) ? ' outline-is-default' : '' }}">
                    {% include '@genesis-admin/layouts/outline.html.twig' %}
                </li>
            {% endfor %}
            {% if genesis.authorize('outline.create') %}
            <li class="card g-block size-1-4 add-new">
                <div class="page">
                    <a href="{{ genesis.route('configurations', 'create') }}"
                       title="{{ 'GENESIS_PLATFORM_ADD_NEW_OUTLINE'|trans }}"
                       aria-label="{{ 'GENESIS_PLATFORM_ADD_NEW_OUTLINE'|trans }}"
                       data-genesis-outline-create tabindex="0">
                        <i class="fa fa-fw fa-plus" aria-hidden="true"></i>
                    </a>
                </div>
            </li>
            {% endif %}
        </ul>

        <h2>{{ 'GENESIS_PLATFORM_SYSTEM_OUTLINES'|trans }}</h2>
        <ul class="g-grid">
            {% for name, title in genesis.outlines.system %}
                {% set preset = genesis.outlines.layoutPreset(name) %}
                <li class="card g-block size-1-4">
                    <div class="page">
                        <h4>{{ title }} <span class="float-right font-small">({{ 'GENESIS_PLATFORM_ID_X'|trans(name) }})</span></h4>
                        <div class="inner-params">
                            <img src="{{ url(preset.image|default('genesis-admin://images/layouts/default.png')) }}" />
                        </div>
                        <div class="inner-params">
                            <div class="center">
                                <a data-tip="{{ 'GENESIS_PLATFORM_EDIT'|trans }}"
                                   title="{{ 'GENESIS_PLATFORM_EDIT_X'|trans(title) }}"
                                   aria-label="{{ 'GENESIS_PLATFORM_EDIT_X'|trans(title) }}"
                                   data-genesis-ajaxify=""
                                   data-genesis-ajaxify-target="[data-genesis-content-wrapper]"
                                   data-genesis-ajaxify-params="{{ {"navbar": true}|json_encode|e('html_attr') }}"
                                   href="{{ genesis.route('configurations', name, 'layout') }}"
                                   class="button button-primary">
                                    <i class="fa fa-fw fa-pencil fa-pencil-alt" aria-hidden="true"></i>
                                </a>
                                {% if genesis.authorize('outline.create') %}
                                <a href="{{ genesis.route('configurations', name, 'duplicate') }}"
                                   data-tip="{{ 'GENESIS_PLATFORM_DUPLICATE'|trans }}"
                                   title="{{ 'GENESIS_PLATFORM_DUPLICATE_X'|trans(title) }}"
                                   aria-label="{{ 'GENESIS_PLATFORM_DUPLICATE_X'|trans(title) }}"
                                   data-genesis-outline-duplicate class="button button-secondary">
                                    <i class="fa fa-fw fa-copy" aria-hidden="true"></i>
                                </a>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
