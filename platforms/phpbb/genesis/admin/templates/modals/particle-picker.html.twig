<div data-mm-particle-stepone="{{ genesis.route(route|default('menu/particle')) }}" class="menu-editor-extras">
    <div class="card settings-block">
        <h4>
            {{ 'GENESIS_PLATFORM_PICK_PARTICLE'|trans }}
        </h4>
        <div class="inner-params">
            <div class="genesis-lm-particles-picker menu-editor-particles">
                <div class="search settings-block">
                    <input type="text" placeholder="{{ 'GENESIS_PLATFORM_SEARCH_ELI'|trans }}" />
                    <i class="fa fa-search" aria-hidden="true"></i>
                </div>
                <ul>
                    {% for key, list in particles %}
                        {{ key|capitalize }}
                        <ul>
                            {% for type, particle in list %}
                                {% for item_key, item in particle %}
                                    <li class="genesis-lm-particle-{{ type }} particle-category-{{ item._genesis_category_slug|default('general') }}"
                                        data-lm-nodrag
                                        data-mm-type="particle"
                                        data-lm-blocktype="{{ type }}"
                                        {% if item._genesis_source|default(null) == 'theme' %}data-lm-source="theme"{% endif %}
                                        data-lm-category="{{ item._genesis_category_slug|default('general') }}"
                                        data-mm-filter="{{ item.name }}"
                                        data-lm-subtype="{{ item_key }}">
                                        <span class="icon"><i class="fa fa-fw {{ item.icon|default('fa-cube') }}" aria-hidden="true"></i></span>
                                        <span class="title">{{ item.name }}</span>
                                    </li>
                                {%  endfor %}
                            {% endfor %}
                        </ul>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
    <div class="g-modal-actions">
        {#<a class="button float-left">{{ 'GENESIS_PLATFORM_DEFAULTS'|trans }}</a>#}
        <button class="button button-primary" type="submit" data-mm-select="" disabled="disabled">
            {{ 'GENESIS_PLATFORM_SELECT'|trans }} <i class="fa fa-fw fa-chevron-right" aria-hidden="true"></i>
        </button>
        <button class="button genesis-dialog-close">
            {{ 'GENESIS_PLATFORM_CANCEL'|trans }}
        </button>
    </div>
</div>
