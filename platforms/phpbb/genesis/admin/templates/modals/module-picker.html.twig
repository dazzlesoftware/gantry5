{# Available variables:

module.[id | title | position | enabled | access]
#}
<div data-mm-particle-stepone="{{ genesis.route('menu/particle') }}" class="menu-editor-extras">
    <div class="card settings-block">
        <h4>
            {{ 'GENESIS_PLATFORM_PICK_MODULE'|trans }}
        </h4>
        <div class="inner-params">
            <div class="genesis-mm-modules-picker menu-editor-modules">
                <div class="search settings-block">
                    <input type="text" placeholder="{{ 'GENESIS_PLATFORM_SEARCH_ELI'|trans }}" />
                    <i class="fa fa-search" aria-hidden="true"></i>
                </div>
                <div class="modules-wrapper">
                    <ul>
                        {% for module in genesis.platform.listModules %}
                        <li data-mm-module="{{ module.id }}" data-mm-type="module">
                            <span class="module-infos">
                                <span data-title="{{ (module.enabled ? 'GENESIS_PLATFORM_PUBLISHED'|trans : 'GENESIS_PLATFORM_UNPUBLISHED'|trans) ~ ' / ' ~ module.access }}"
                                      data-tip="{{ (module.enabled ? 'GENESIS_PLATFORM_PUBLISHED'|trans : 'GENESIS_PLATFORM_UNPUBLISHED'|trans) ~ ' / ' ~ module.access }}"
                                      data-tip-place="top-left"
                                >
                                    <i class="fa fa-fw fa-{{ module.enabled ? 'toggle-on' : 'toggle-off' }}" aria-hidden="true"></i>
                                </span>
                            </span>
                            <span class="module-wrapper">
                                <span class="title" data-mm-title="{{ module.title }}" data-mm-filter="{{ module.title }}">
                                    {{ module.title }}
                                </span>
                                <span class="sub-title font-small">
                                    <strong>{{ 'GENESIS_PLATFORM_ID_UPPER'|trans }}</strong>: {{ module.id|default('unknown') }}
                                </span>
                                <span class="sub-title font-small" data-mm-filter="{{ module.module|default('none') }}">
                                    <strong>{{ 'GENESIS_PLATFORM_TYPE'|trans }}</strong>: {{ module.module|default('none') }}
                                </span>
                                <span class="sub-title font-small">
                                    <strong>{{ 'GENESIS_PLATFORM_POSITION'|trans }}</strong>: {{ module.position|default('none') }}
                                </span>
                            </span>
                        </li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <div class="g-modal-actions">
        {#<a class="button float-left">{{ 'GENESIS_PLATFORM_DEFAULTS'|trans }}</a>#}
        <button class="button button-primary" type="submit" data-mm-select disabled>{{ 'GENESIS_PLATFORM_SELECT'|trans }}</button>
        <button class="button genesis-dialog-close">{{ 'GENESIS_PLATFORM_CANCEL'|trans }}</button>
    </div>
</div>
