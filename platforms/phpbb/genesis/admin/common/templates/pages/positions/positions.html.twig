{% extends ajax-suffix ? "@genesis-admin/partials/ajax.html.twig" : "@genesis-admin/partials/base.html.twig" %}

{% block genesis %}
    <div class="menu-header">
        <span class="float-right">
            <button class="button button-back-to-conf"
                    tabindex="0"
                    aria-label="{{ 'GENESIS_PLATFORM_BACK_SETUP'|trans }}"
            >
                <i class="fa fa-fw fa-fw fa-arrow-left" aria-hidden="true"></i> <span>{{ 'GENESIS_PLATFORM_BACK_SETUP'|trans }}</span>
            </button>
            <button type="submit"
                    class="button button-primary button-save"
                    data-save="{{ 'GENESIS_PLATFORM_POSITIONS'|trans }}"
            >
                <i class="fa fa-fw fa-check" aria-hidden="true"></i> <span>{{ 'GENESIS_PLATFORM_SAVE_POSITIONS'|trans }}</span>
            </button>
        </span>
        <h2 class="page-title">
            {{ 'GENESIS_PLATFORM_POSITIONS'|trans }}
            <button data-g-config="create"
                    data-g-create="position"
                    data-g-config-href="{{ genesis.route('positions/create') }}"
                    title="{{ 'GENESIS_PLATFORM_ADD_NEW_POSITION'|trans }}"
                    aria-label="{{ 'GENESIS_PLATFORM_ADD_NEW_POSITION'|trans }}"
                    data-g-config-method="POST"
                    data-genesis-lm-add tabindex="0"
                    class="button button-primary"
            >
                <i class="fa fa-fw fa-plus" aria-hidden="true"></i>
            </button>
        </h2>
    </div>

    <div id="positions">
        <ul class="g-grid">
            {% for name, position in positions %}
                <li class="card g-block admin-card-quarter">
                    {% include '@genesis-admin/layouts/position.html.twig' %}
                </li>
            {% endfor %}
            <li class="card g-block admin-card-quarter add-new">
                <div class="page">
                    <a data-g-config="create"
                       data-g-config-href="{{ genesis.route('positions/create') }}"
                       title="{{ 'GENESIS_PLATFORM_ADD_NEW_POSITION'|trans }}"
                       aria-label="{{ 'GENESIS_PLATFORM_ADD_NEW_POSITION'|trans }}"
                       data-g-config-method="POST"
                       data-genesis-lm-add tabindex="0"
                    >
                        <i class="fa fa-fw fa-plus" aria-hidden="true"></i>
                    </a>
                </div>
            </li>
        </ul>
        <div id="trash" data-genesis-positions-erase=""><div class="trash-zone">&times;</div><span>{{ 'GENESIS_PLATFORM_DROP_DELETE'|trans }}</span></div>
    </div>
{% endblock %}
